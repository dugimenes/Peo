apiVersion: apps/v1
kind: Deployment
metadata:
  name: gestao-alunos-api
  namespace: peo-platform
  labels:
    app: gestao-alunos-api
    tier: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: gestao-alunos-api
  template:
    metadata:
      labels:
        app: gestao-alunos-api
        tier: backend
    spec:
      containers:
      - name: gestao-alunos-api
        image: <DOCKER_USERNAME>/peo-gestao-alunos-api:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8080
          name: http
        env:
        - name: ConnectionStrings__DefaultConnection
          value: "Server=sqlserver-service,1433;Database=PeoGestaoAlunosDb;User Id=sa;TrustServerCertificate=True;Password=$(DB_PASSWORD)"
        - name: DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: peo-secrets
              key: DB_PASSWORD
        - name: JwtOptions__SecretKey
          valueFrom:
            secretKeyRef:
              name: peo-secrets
              key: JWT_SECRET_KEY
        envFrom:
        - configMapRef:
            name: peo-config
        resources:
          requests:
            memory: "256Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /alive
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
---
apiVersion: v1
kind: Service
metadata:
  name: gestao-alunos-api-service
  namespace: peo-platform
  labels:
    app: gestao-alunos-api
spec:
  type: ClusterIP
  selector:
    app: gestao-alunos-api
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
